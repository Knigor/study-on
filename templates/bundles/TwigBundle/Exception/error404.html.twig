{% extends 'base.html.twig' %}

{% block title %}
    {{ status }}/Stydy-On
{% endblock %}

{% block body %}

    <div style="display: flex; flex-direction: column; align-items: center; margin-top: 3rem; gap: 30px;">
        <h1>Ошибка <span class="text-danger">{{ status }}</span></h1>
        <div style="width: 500px; font-size: 20px; text-align:center;">
            Страницы не существует
        </div>
        <a href="{{ path('app_course_index') }}" class="btn btn-outline-success">Вернуться на главную страницу</a>
    </div>

{% endblock %}