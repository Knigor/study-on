{% block title %}Профиль пользователя{% endblock %}

{% block body %}
    <h1>Профиль</h1>
    <div class="card">
        <ul class="list-group list-group-flush">
            <li class="list-group-item username"><strong>Почта:</strong> {{ user.username }}</li>
            <li class="list-group-item roles"><strong>Роли:</strong>
                <ul>
                    {% for role in user.roles %}
                        <li class="role">{{ role }}</li>
                    {% endfor %}
                </ul>
            </li>
            <li class="list-group-item balance"><strong>Баланс:</strong> {{ user.balance }}</li>
        </ul>
    </div>
{% endblock %}