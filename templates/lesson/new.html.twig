{% extends 'base.html.twig' %}

{% block title %}Создание нового урока{% endblock %}

{% block body %}
    <div class="container mt-5">
        <h1 class="text-center mb-4">Создание нового урока</h1>

        {{ include('lesson/_form.html.twig') }}

        <div class="mt-3 text-center">
            <a href="{{ path('app_course_show', {'id': course.id}) }}" class="btn btn-secondary">Вернуться к курсу</a>
        </div>
    </div>
{% endblock %}
